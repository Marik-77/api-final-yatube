# Yatube API 🚀

REST API для социальной сети **Yatube** - платформы для обмена постами, комментариями и отслеживания других пользователей.

## 📋 Содержание

- [Описание](#описание)
- [Технологии](#технологии)
- [Требования](#требования)
- [Установка](#установка)
- [Использование](#использование)
- [API Endpoints](#api-endpoints)
- [Аутентификация](#аутентификация)
- [Тестирование](#тестирование)
- [Структура проекта](#структура-проекта)

## 📝 Описание

Yatube API предоставляет полнофункциональный REST API для управления:

- **Постами** - создание, чтение, редактирование и удаление постов
- **Комментариями** - комментирование постов других пользователей
- **Группами** - организация постов по категориям
- **Подписками** - отслеживание постов интересующих пользователей
- **Аутентификацией** - безопасный доступ через JWT токены

## 🛠 Технологии

- **Django** 5.1.1 - веб-фреймворк
- **Django REST Framework** 3.15.2 - создание REST API
- **Simple JWT** - аутентификация через JWT токены
- **Djoser** - управление пользователями и аутентификацией
- **Pillow** - обработка изображений
- **Pytest** - тестирование API
- **Social Auth** - социальная аутентификация

## 📦 Требования

- Python 3.8+
- pip
- Django 5.1.1
- Все зависимости из `requirements.txt`

## 💻 Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd api-final-yatube
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Примените миграции

```bash
cd yatube_api
python manage.py makemigrations
python manage.py migrate
```

### 5. Создайте суперпользователя

```bash
python manage.py createsuperuser
```

### 6. Запустите сервер разработки

```bash
python manage.py runserver
```

Сервер будет доступен по адресу `http://localhost:8000/`

## 🚀 Использование

### Документация API

После запуска сервера документация доступна по адресу:
- **ReDoc** - `http://localhost:8000/redoc/`
- **Swagger** - `http://localhost:8000/api/schema/swagger-ui/`
- **Admin панель** - `http://localhost:8000/admin/`

### Postman коллекция

В папке `postman_collection/` находится готовая коллекция Postman для тестирования API:

```bash
cd postman_collection
bash set_up_data.sh  # Подготовит тестовые данные
```

Затем импортируйте `API_for_yatube.postman_collection.json` в Postman.

## 🔌 API Endpoints

### Посты
```
GET    /api/v1/posts/              # Список всех постов
POST   /api/v1/posts/              # Создать пост
GET    /api/v1/posts/{id}/         # Получить пост
PUT    /api/v1/posts/{id}/         # Обновить пост
DELETE /api/v1/posts/{id}/         # Удалить пост
```

### Комментарии
```
GET    /api/v1/posts/{post_id}/comments/              # Комментарии поста
POST   /api/v1/posts/{post_id}/comments/              # Добавить комментарий
GET    /api/v1/posts/{post_id}/comments/{comment_id}/ # Получить комментарий
PUT    /api/v1/posts/{post_id}/comments/{comment_id}/ # Обновить комментарий
DELETE /api/v1/posts/{post_id}/comments/{comment_id}/ # Удалить комментарий
```

### Группы
```
GET    /api/v1/groups/       # Список всех групп
GET    /api/v1/groups/{id}/  # Получить группу
```

### Подписки
```
GET    /api/v1/follow/  # Список моих подписок
POST   /api/v1/follow/  # Подписаться на пользователя
```

### Аутентификация
```
POST   /api/v1/jwt/create/      # Получить JWT токены
POST   /api/v1/jwt/refresh/     # Обновить access токен
POST   /api/v1/jwt/verify/      # Проверить токен
```

## 🔐 Аутентификация

API использует **JWT (JSON Web Tokens)** для аутентификации.

### Получение токена

```bash
curl -X POST http://localhost:8000/api/v1/jwt/create/ \
  -H "Content-Type: application/json" \
  -d '{"username": "user", "password": "password"}'
```

### Использование токена

Передайте токен в заголовке Authorization:

```bash
curl -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  http://localhost:8000/api/v1/posts/
```

## 🧪 Тестирование

### Запуск всех тестов

```bash
pytest
```

### Запуск конкретного набора тестов

```bash
pytest tests/test_post.py          # Тесты постов
pytest tests/test_comment.py       # Тесты комментариев
pytest tests/test_follow.py        # Тесты подписок
pytest tests/test_group.py         # Тесты групп
pytest tests/test_jwt.py           # Тесты аутентификации
```

### С выводом покрытия

```bash
pytest --cov=api --cov=posts
```

## 📁 Структура проекта

```
api-final-yatube/
├── yatube_api/              # Django проект
│   ├── api/                 # REST API приложение
│   │   ├── views.py         # ViewSets для эндпоинтов
│   │   ├── serializers.py   # Сериализаторы моделей
│   │   ├── permissions.py   # Права доступа
│   │   ├── urls.py          # URL маршруты
│   │   └── templates/       # HTML шаблоны
│   ├── posts/               # Приложение моделей
│   │   ├── models.py        # Модели: Post, Comment, Group, Follow
│   │   └── migrations/      # Миграции БД
│   └── yatube_api/          # Конфигурация проекта
│       ├── settings.py      # Параметры приложения
│       ├── urls.py          # Основные URL маршруты
│       └── wsgi.py          # WSGI конфигурация
├── tests/                   # Тесты API
│   ├── test_post.py
│   ├── test_comment.py
│   ├── test_follow.py
│   ├── test_group.py
│   ├── test_jwt.py
│   └── fixtures/            # Тестовые данные
├── postman_collection/      # Postman коллекция
└── requirements.txt         # Зависимости проекта
```

## 🔧 Основные модели

### Post (Пост)
- `text` - содержание поста
- `author` - автор поста
- `pub_date` - дата публикации
- `image` - изображение (опционально)
- `group` - группа (опционально)

### Comment (Комментарий)
- `text` - содержание комментария
- `author` - автор комментария
- `post` - пост, к которому относится комментарий
- `created` - дата создания

### Group (Группа)
- `title` - название группы
- `slug` - URL-friendly идентификатор
- `description` - описание группы

### Follow (Подписка)
- `user` - подписчик
- `following` - пользователь, на которого подписываются

## 📖 Дополнительная информация

### Пермиссии

- **IsAuthenticatedOrReadOnly** - аутентифицированные пользователи могут писать, остальные могут только читать
- **IsOwnerOrReadOnlyPermission** - только владелец может редактировать/удалять
- **IsAuthenticated** - только аутентифицированные пользователи

### Пагинация

API использует `LimitOffsetPagination`:

```bash
GET /api/v1/posts/?limit=10&offset=0
```

### Фильтрация

Подписки поддерживают поиск по имени пользователя:

```bash
GET /api/v1/follow/?search=username
```
